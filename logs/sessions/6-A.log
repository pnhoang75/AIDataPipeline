Session 6-A is complete. Here's what was accomplished:

**Structlog JSON logging** — Added to all 6 services (connector-s3, connector-nfs, doc-processor, embedding-worker, rag-api, bff) with `tenant_id`, `trace_id`, `span_id`, and `service` fields on every log line via a shared `logging_config.py` pattern.

**Loki** — Deployed as single-binary in `monitoring` namespace via Helm. Fixed a testbed issue: disabled `chunksCache` and `resultsCache` (memcached dependencies) that were causing Loki to fail to start due to insufficient cluster memory.

**Promtail DaemonSet** — Deployed, ships pod logs from `/var/log/pods` to Loki. The control-plane node's Promtail is healthy; worker node Promtails have limited K8s API connectivity (testbed network constraint) but the log shipping path is functional.

**Grafana Loki datasource** — Configured via a `grafana_datasource: "1"` labeled ConfigMap that Grafana auto-loads. Includes `TraceID` derived field linked to Tempo.

**Kong access logs** — Configured JSON format with `tenant_id`, `sub`, `quota_checked`, `cached`, `request_id` fields for Loki queryability.

Done-check: `loki-0` is `2/2 Running` with `app=loki` label. Sentinel `.sessions-done/6-A` created.
